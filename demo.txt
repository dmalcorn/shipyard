python -m src.main --cli

Surgical edit (happy path):  
In sandbox/hello.py, replace "Hello, {name}!" with "Hi there, {name}!"

Fail-loud (no match): 
In sandbox/hello.py, replace "def validate_path" with "def check_path"

Read + summarize:
Read sandbox/hello.py and describe what functions it contains

YAML edit:
In sandbox/config_sample.yaml, change debug: true to debug: false
In sandbox/config_sample.yaml, change debug: false to debug: true



health check on deployed app:  curl https://shipyard-production-29ae.up.railway.app/health

You need to start the server first:   python -m src.main

this starts the FastAPI server on port 8000. Then to show context injection, in a separate terminal, run the curl command:

curl -X POST http://localhost:8000/instruct -H "Content-Type: application/json" -d '{"message": "Read sandbox/config_sample.yaml. The deployment team has told us the database port must change from 5433 to 5432 and debug must be set to true. Make both changes."}'

langGraph traces