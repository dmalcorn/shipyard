# AI Prompts — 2026-03-24

## 10:13 AM CDT

Apply code review fixes for Epic 1 from fix plan file `/c/alcorn/Gauntlet/6-Shipyard/shipyard/looper/epic1-code-review-fix-plan.md`. Read CLAUDE.md and coding-standards.md first, apply fixes in priority order (P0-P3), log each fix to execution log file, then run verification (pytest, ruff, mypy).

## 10:18 AM CDT

please review the bash scripts in the looper folder so analyze if they can be used in this project. [followed by confirmation "yes", file permission check, then pasted run output showing Phase 4 fails when architect phase had no items]

the script continued, but as you can see, there is an improvement to the script that needs to be done. can you look at the failure scenario in the latest run, and put in some logic that says If the architect is not needed on the second loop, then the loop shouldn't fail, it should just continue.

## 10:26 AM CDT

so I want to restart this with just the epic number as the input, and I want to pick up where it left off, but that didn't happen. I know there are parameters that can be entered on the command line, but I also want the script to know where to restart from if there are no extra parameters on the command line (besides the epic number). can you look at the logic used for the .json state file and the states of the different phases to see if this can be implemented? show me your analysis first. don't make changes yet

## 10:30 AM CDT

yes [implement the resume-from-state-file feature]

## 10:33 AM CDT

Fix CI failures for Epic 1 in the Shipyard project.

CRITICAL FIRST STEP: Read /c/alcorn/Gauntlet/6-Shipyard/shipyard/CLAUDE.md and /c/alcorn/Gauntlet/6-Shipyard/shipyard/coding-standards.md for coding rules.

CI ISSUES FILE: /c/alcorn/Gauntlet/6-Shipyard/shipyard/looper/epic1-ci-issues-attempt-1.txt

Fix all lint errors (ruff), type errors (mypy), and test failures (pytest).

After fixing, output:
=== CI FIXES APPLIED ===
- [LINT/TEST/TYPE] Brief description (file:line)
=== END CI FIXES ===

Begin by reading CLAUDE.md and the CI issues file now.

## 10:40 AM CDT

please put a check at the beginning of this script for the software products that are required to successfully run the entire script. In particular, it looks like there were some missing pieces of software that caused the CI to fail, but it would be nice to have a check at the top. If the software doesn't exist, can the script install the correct pieces of software? [pasted CI output showing ruff/mypy/pytest not on PATH, fixed by python -m invocations]

## 10:58 AM CDT

/bmad-dev

## 11:28 AM CDT

looks like the script does not actually step through all the stages. Please see this log. [pasted output showing script exits silently after check_dependencies — no phases run]

## 11:36 AM CDT

check the log files for the epic2-code-review-claude.log. The script for this log file was running fine this morning before we started making all these changes to these state files. Can you look to see what might have caused this failure to run?
